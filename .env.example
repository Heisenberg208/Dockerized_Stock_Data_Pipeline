# Database for your ETL
POSTGRES_HOST=postgres
POSTGRES_DB=stockdata
POSTGRES_USER=airflow
POSTGRES_PASSWORD=airflow123
PGADMIN_DEFAULT_EMAIL= admin@admin.com
PGADMIN_DEFAULT_PASSWORD= admin

# Airflow config
AIRFLOW__CORE__EXECUTOR=CeleryExecutor
AIRFLOW__CORE__DAGS_ARE_PAUSED_AT_CREATION=False
AIRFLOW__CORE__FERNET_KEY=REPLACE_ME  # 🔐 Run: `python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"`
AIRFLOW__WEBSERVER__SECRET_KEY=temporary-dev-key

# IMPORTANT: Use a dedicated "airflow" DB for Airflow metadata
AIRFLOW__DATABASE__SQL_ALCHEMY_CONN=postgresql+psycopg2://airflow:airflow123@postgres:5432/airflow
AIRFLOW__CELERY__RESULT_BACKEND=db+postgresql://airflow:airflow123@postgres:5432/airflow
AIRFLOW__CELERY__BROKER_URL=redis://redis:6379/0

# Default connection for your pipeline (points to stockdata)
AIRFLOW_CONN_POSTGRES_DEFAULT=postgresql://airflow:airflow123@postgres:5432/stockdata


